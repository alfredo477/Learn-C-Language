动态内存分配之后需要释放内存并把指针定义为0
malloc:void* (size_t size) 申请一块内存地址 所以只需要长度
 1.一个参数 (初始化)默认为随机值
calloc:void* (size_t num,size_t size) 申请一块内存地址 但是初始化默认为0
 1.两个参数 函数默认值为0
realloc:void* (void* ptr,size_t size) 为申请的地址 重分配大小 需要原地址和长度
 1.两个参数 一个是原来的指针地址 另一个是调整之后的大小
 2.调整的方式有两种(详情略)